1：finish 需要取消注释
2：清单文件需要添加 系统属性

    public void initOther() {
        Intent intent = new Intent(PoliceActivity.this, ScreenViewService.class);
        startService(intent);
//        finish();
    }